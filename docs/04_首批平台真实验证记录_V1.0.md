# 首批周刊原创发布渠道验证记录 V1.0

## 验证前置状态

- 用户要求的 `samples/known_original_and_reprints.txt` 在当前仓库不存在，不能据此认定任何链接为 `KNOWN_REPRINTS`。
- 当前可见样本为 `samples/原创链接.txt`；业务方已确认其中链接均为贵方自己发布的原创。它们只可用于“原创源排除/非转载”验证，不能作为转载成功证据。
- 另有 `tests/fixtures/publisher_channel_validation_samples.json`：仅保存业务方提供的 5 条本机验证输入，显式标记为 `UNSPECIFIED_TEST_DATA`，不进入生产配置或原创判定。
- 本机浏览器验证连接在初始化阶段失败，未产生 DevTools Network/XHR 记录；因此搜索入口、参数、分页与页面字段仍为“待验证”。
- 已通过统一安全 HTTP 客户端对业务方提供的 5 条直接 URL 完成本机请求验证，真实输出保存在 `docs/evidence/2026-08-07_publisher_url_probe.json`。这不是搜索适配器验证，也不将 URL 写入生产平台配置或文章分类。

## 本机直接 URL 验证结果

| 渠道 | HTTP | 标题文字比对 | 当前适配状态 | 证据 |
| --- | --- | --- | --- | --- |
| 周刊官网 | 200 | 页面标题带渠道后缀，字面不相等 | 仅直接 URL 验证；搜索适配待验证 | `docs/evidence/2026-08-07_publisher_url_probe.json` |
| 同花顺 | 200 | 相等 | 仅直接 URL 验证；搜索适配待验证 | 同上 |
| 东方财富网 | 200 | 页面标题带渠道后缀，字面不相等 | 仅直接 URL 验证；搜索适配待验证 | 同上 |
| 新浪财经 | 200 | 页面标题带渠道后缀，字面不相等 | 仅直接 URL 验证；搜索适配待验证 | 同上 |
| 中金在线 | 200 | 页面标题带渠道后缀，字面不相等 | 仅直接 URL 验证；搜索适配待验证 | 同上 |

## 统一证据目录

- 浏览器 Network/HAR（脱敏后）：`evidence/platforms/<platform>/`，尚未创建。
- 真实页面/JSON 响应摘要：`evidence/platforms/<platform>/response-summary.md`，尚未创建。
- 正负样本截图：`evidence/platforms/<platform>/screenshots/`，尚未创建。

## 平台卡片

### 周刊官网

| 字段 | 当前记录 |
| --- | --- |
| 平台名称 | 周刊官网 |
| 候选域名 | `static.weeklyonstock.com`（已通过直接 URL 本机验证；搜索域名仍待验证） |
| 搜索入口、请求方式、参数、分页 | 待验证 |
| 标题、链接、发布时间字段 | 待验证 |
| 动态加载、Playwright | 待验证 |
| 请求间隔、超时 | 待验证；实施时由数据库平台配置决定 |
| 成功/失败测试样本 | 待验证；指定 KNOWN_REPRINTS 样本文件缺失 |
| 最近验证时间、当前适配状态 | 2026-08-07；仅直接 URL 验证，搜索适配待验证 |

### 同花顺

| 字段 | 当前记录 |
| --- | --- |
| 平台名称 | 同花顺 |
| 候选域名 | `stock.10jqka.com.cn`（已通过直接 URL 本机验证；搜索域名仍待验证） |
| 搜索入口、请求方式、参数、分页 | 待验证 |
| 标题、链接、发布时间字段 | 待验证 |
| 动态加载、Playwright | 待验证 |
| 请求间隔、超时 | 待验证；实施时由数据库平台配置决定 |
| 成功/失败测试样本 | 待验证；指定 KNOWN_REPRINTS 样本文件缺失 |
| 最近验证时间、当前适配状态 | 2026-08-07；仅直接 URL 验证，搜索适配待验证 |

### 东方财富网

| 字段 | 当前记录 |
| --- | --- |
| 平台名称 | 东方财富网 |
| 候选域名 | `finance.eastmoney.com`（已通过直接 URL 本机验证；搜索域名仍待验证） |
| 搜索入口、请求方式、参数、分页 | 待验证 |
| 标题、链接、发布时间字段 | 待验证 |
| 动态加载、Playwright | 待验证 |
| 请求间隔、超时 | 待验证；实施时由数据库平台配置决定 |
| 成功/失败测试样本 | 待验证；指定 KNOWN_REPRINTS 样本文件缺失 |
| 最近验证时间、当前适配状态 | 2026-08-07；仅直接 URL 验证，搜索适配待验证 |

### 新浪财经

| 字段 | 当前记录 |
| --- | --- |
| 平台名称 | 新浪财经 |
| 候选域名 | `finance.sina.com.cn`（已通过直接 URL 本机验证；搜索域名仍待验证） |
| 搜索入口、请求方式、参数、分页 | 待验证 |
| 标题、链接、发布时间字段 | 待验证 |
| 动态加载、Playwright | 待验证 |
| 请求间隔、超时 | 待验证；实施时由数据库平台配置决定 |
| 成功/失败测试样本 | 待验证；指定 KNOWN_REPRINTS 样本文件缺失 |
| 最近验证时间、当前适配状态 | 2026-08-07；仅直接 URL 验证，搜索适配待验证 |

### 中金在线

| 字段 | 当前记录 |
| --- | --- |
| 平台名称 | 中金在线 |
| 候选域名 | `sc.stock.cnfol.com`（已通过直接 URL 本机验证；搜索域名仍待验证） |
| 搜索入口、请求方式、参数、分页 | 待验证 |
| 标题、链接、发布时间字段 | 待验证 |
| 动态加载、Playwright | 待验证 |
| 请求间隔、超时 | 待验证；实施时由数据库平台配置决定 |
| 成功/失败测试样本 | 待验证；指定 KNOWN_REPRINTS 样本文件缺失 |
| 最近验证时间、当前适配状态 | 2026-08-07；仅直接 URL 验证，搜索适配待验证 |
